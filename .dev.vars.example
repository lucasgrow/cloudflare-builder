# Required — generated automatically by `bun run setup`
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_RESEND_KEY=
AUTH_EMAIL_FROM=noreply@yourdomain.com

# Optional — only needed for presigned R2 uploads
# Get credentials: CF dashboard → R2 → Manage R2 API Tokens → Create API Token
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ACCOUNT_ID=
R2_BUCKET_NAME=

# Creative Post — AI keys
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=

# Google OAuth redirect URI (add this in Google Cloud Console):
# http://localhost:3000/api/auth/callback/google
